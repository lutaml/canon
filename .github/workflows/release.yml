# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
# 2026-08-27: publish via RubyGems OIDC trusted publishing (no API key
# secret); direct call to metanorma/ci instead of the relaton/support
# wrapper, which forwards the API-key secret unconditionally.
name: release

permissions:
  contents: write
  packages: write
  id-token: write

on:
  workflow_dispatch:
    inputs:
      next_version:
        description: |
          Next release version. Possible values: x.y.z, major, minor, patch (or pre|rc|etc).
          Also, you can pass 'skip' to skip 'git tag' and do 'gem push' for the current version
        required: true
        default: 'skip'
  repository_dispatch:
    types: [ do-release ]

jobs:
  release:
    uses: metanorma/ci/.github/workflows/rubygems-release.yml@main
    with:
      next_version: ${{ github.event.inputs.next_version }}
